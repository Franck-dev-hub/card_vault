services:
  backend:
    build: ./backend
  frontend:
    build: ./frontend
  ml_service:
    build: ./ml_service
  proxy:
    build: ./proxy
    volumes:
      - ./proxy/default.dev.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
